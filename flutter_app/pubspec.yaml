name: flutter_application
description: "A new Flutter project for a budgeting app."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  provider: ^6.1.5
  http: ^1.2.0
  fl_chart: ^1.1.1
  counter: ^0.2.2
  counter_app: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.26.2

flutter:
  uses-material-design: true
